import { FLIGHT_TYPE } from "@/types/component-types";

export const FLIGHT_TYPES_LABELS = {
    [FLIGHT_TYPE.ONE_WAY]: 'One Way Flight',
    [FLIGHT_TYPE.RETURN]: 'Return Flight',
};

export const TIMER_LABELS = {
    ELAPSED_TIME: 'Elapsed Time:',
    DURATION: 'Duration:',
};